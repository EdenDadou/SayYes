generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Portfolio {
  id              String   @id @default(cuid())
  titre           String
  categories      String   @default("[]")
  slug            String   @unique
  photoCouverture String
  photoMain       String   @default("")
  description     String   @default("")
  kicker          String   @default("")
  sousTitre       String   @default("")
  topTitle        String   @default("")
  couleur         String   @default("")
  temoignage      String   @default("{\"auteur\":\"\",\"contenu\":\"\"}")
  livrable        String   @default("[]")
  bento           String   @default("[]")
  metaTitle       String   @default("")
  metaDescription String   @default("")
  metaImage       String   @default("")
  schemaOrg       String   @default("{}")
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
  medias          Media[]

  @@map("portfolios")
}

model Media {
  id           String     @id @default(cuid())
  filename     String
  originalName String
  mimetype     String
  size         Int
  path         String
  url          String
  type         MediaType
  createdAt    DateTime   @default(now())
  portfolioId  String?
  portfolio    Portfolio? @relation(fields: [portfolioId], references: [id], onDelete: Cascade)

  @@map("medias")
}

model Solution {
  id          String   @id @default(cuid())
  titre       String
  description String
  image       String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@map("solutions")
}

enum MediaType {
  IMAGE
  VIDEO
  DOCUMENT
}
